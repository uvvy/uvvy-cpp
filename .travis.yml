language: c++
before_install:
  - sudo add-apt-repository ppa:h-rayflood/llvm
  - sudo apt-get update
  - sudo apt-get install -qq libasound2-dev libboost-test-dev libboost-serialization-dev libboost-system-dev libboost-date-time-dev clang-3.2
script: ./scripts/travis_build.sh
notifications:
  email: false
